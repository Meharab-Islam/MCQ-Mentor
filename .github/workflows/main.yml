name: Flutter Minimal Build Only

# Workflowটি কখন ট্রিগার হবে
on:
  pull_request:
    branches:
      - main
      - master
      - developer
  push:
    branches:
      - main
      - master
      - developer

jobs:
  # Single Job: শুধুমাত্র APK এবং IPA ফাইল তৈরি করবে
  minimal_build:
    name: 🏗️ Build APK
    runs-on: macos-latest # iOS বিল্ডের জন্য macos রানার আবশ্যক
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # 1. Set up Flutter SDK (MANDATORY)
      - name: Set up Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      # 2. Set up Java (for Android Build)
      # Android APK তৈরি করার জন্য Java 17 প্রয়োজন
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17' 
          
      - name: Install dependencies (MANDATORY before build)
        run: flutter pub get

      # --- 3. Build APK ---
      - name: Build Android APK
        run: flutter build apk --release --split-per-abi

      # Artifacts আপলোড (ঐচ্ছিক, ফাইলগুলো GitHub-এ সংরক্ষণের জন্য)
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-release-apk
          path: build/app/outputs/apk/release
          
        with:
          name: ios-release-ipa
          path: build/ios/ipa
