name: Flutter Minimal Build Only

# Workflowটি কখন ট্রিগার হবে
on:
  pull_request:
    branches:
      - main
      - master
      - developer
  push:
    branches:
      - main
      - master
      - developer

jobs:
  # Single Job: শুধুমাত্র APK ফাইল তৈরি করবে
  minimal_build:
    name: 🏗️ Build APK
    runs-on: ubuntu-latest # iOS বিল্ড না থাকার কারণে macos-এর পরিবর্তে Ubuntu ব্যবহার করা হলো (কম খরচে দ্রুত রান হয়)
    
    # নতুন সংযোজন: এই জবের জন্য নির্দিষ্ট পারমিশন সেট করা হলো
    permissions:
      contents: write # GitHub Release তৈরি করার জন্য এই পারমিশন আবশ্যক

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # 1. Set up Flutter SDK (MANDATORY)
      - name: Set up Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      # 2. Set up Java (for Android Build)
      # Android APK তৈরি করার জন্য Java 17 প্রয়োজন
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17' 
          
      - name: Install dependencies (MANDATORY before build)
        run: flutter pub get

      # --- 3. Build APK ---
      - name: Build Android APK
        run: flutter build apk --release --split-per-abi

      # Artifacts আপলোড (ঐচ্ছিক, ফাইলগুলো GitHub-এ সংরক্ষণের জন্য)
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-release-apk
          path: build/app/outputs/apk/release

      # 4. GitHub Release তৈরি করা হলো (নতুন ধাপ)
      - name: Create GitHub Release
        # শুধুমাত্র main বা master এ push হলেই রিলিজ তৈরি হবে
        if: startsWith(github.ref, 'refs/heads/main') || startsWith(github.ref, 'refs/heads/master')
        uses: softprops/action-gh-release@v1
        with:
          # বিল্ট-ইন GITHUB_TOKEN ব্যবহার করা হলো
          token: ${{ secrets.GITHUB_TOKEN }}
          # ট্যাগ নেম সেট করা হলো (যেমন: v1.0.1, v1.0.2 ইত্যাদি)
          tag_name: v1.0.${{ github.run_number }}
          name: Release v1.0.${{ github.run_number }} (${{ github.ref_name }})
          body: Automated APK build for branch `${{ github.ref_name }}`.
          draft: true # প্রথমে Draft রিলিজ হিসেবে তৈরি হবে
          prerelease: ${{ github.ref_name != 'main' && github.ref_name != 'master' }} # main/master ছাড়া অন্য ব্রাঞ্চ (যেমন developer) এর জন্য Prerelease
          files: build/app/outputs/apk/release/*.apk
